# Protocol Package

Shared contracts between backend and frontend.

Planned contents:

- Go DTOs used by `apps/pactmigrate-cli` REST API
- generated JSON schema / TypeScript types for dashboard
- versioned API contract docs
