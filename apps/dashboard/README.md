# Dashboard App

React + Tailwind dashboard for PactMigrate.

Current responsibilities:

- migration run history
- run triggers
- environment visibility

Tech stack:

- React (Vite)
- Tailwind CSS
- shadcn/ui-style components (local `src/components/ui`)

Run:

```bash
make run-dashboard
```
